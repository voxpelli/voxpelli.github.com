{
  "private": true,
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "eslint": "^6.0.1",
    "eslint-config-standard": "^12.0.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.0",
    "husky": "^3.0.0"
  },
  "scripts": {
    "build": "ELEVENTY_ENV=production eleventy",
    "watch": "ELEVENTY_ENV=development eleventy --serve",
    "test": "eslint ."
  },
  "husky": {
    "hooks": {
      "pre-push": "npm test"
    }
  }
}
