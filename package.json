{
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >=24"
  },
  "scripts": {
    "build": "domstack --copy images --copy media --copy static && cp sw.js public/sw.js",
    "dev": "domstack --watch --copy images --copy media --copy static",
    "build:drafts": "domstack --drafts --copy images --copy media --copy static",
    "test": "npm run check:lint",
    "check:lint": "eslint ."
  },
  "devDependencies": {
    "@domstack/static": "^11",
    "@voxpelli/eslint-config": "^22",
    "eslint": "^9",
    "gray-matter": "^4.0.3",
    "husky": "^9"
  }
}
